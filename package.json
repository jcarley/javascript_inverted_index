{
  "name": "invertedindex",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "mocha --ui qunit --require babel-polyfill --compilers js:babel-register",
    "test-watch": "mocha --watch --ui qunit --require babel-polyfill --compilers js:babel-register"
  },
  "author": "Jefferson Carley",
  "license": "ISC",
  "dependencies": {
    "babel-cli": "^6.23.0",
    "babel-preset-env": "^1.1.11",
    "bintrees": "^1.0.1",
    "debug": "^2.6.1",
    "lodash": "^4.17.4"
  },
  "devDependencies": {
    "babel-polyfill": "^6.23.0",
    "babel-register": "^6.23.0",
    "chai": "^3.5.0",
    "faker": "^4.1.0",
    "mocha": "^3.2.0"
  },
  "babel": {
    "presets": [
      [
        "env",
        {
          "targets": {
            "node": "current"
          }
        }
      ]
    ]
  }
}
